---
layout: 'base.njk'
---
       {# Below is the template we want all blog pages to use. To connect this, go to blog.json. Anyything there will render on every blog page... I think #}
<article class="main-article">
<div class="container container--narrow flow">
  
    <h1 class="article-title">
      {{ title }}      {# This is being inserted somehow. Front matter? #}
    </h1>

    <figure class="main-article__figure">
      <img src="{{ image }}" alt="{{ imageAlt }} ">
      <figcaption>
      {{ imageAlt }}
      </figcaption>
    </figure>

    {{ content | safe }} <!-- This will be injected into the base layout^ -->
    
  
</div>
</article>